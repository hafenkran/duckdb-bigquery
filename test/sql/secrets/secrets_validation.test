# name: test/sql/secrets/secrets_validation.test
# description: Test BigQuery secret validation
# group: [secrets]

require bigquery

statement ok
SET bq_auth_timeout_s=1;

statement error
SET bq_auth_timeout_s=-1;
----
Invalid Input Error: Auth timeout must be non-negative

statement ok
SET bq_auth_timeout_s=10;

#################################################################################

statement ok
CREATE SECRET bq_auth_preflight_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://auth-preflight-project',
    REFRESH_TOKEN '1//test-refresh-token',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com',
    CLIENT_SECRET 'test-client-secret',
    TOKEN_URI 'http://127.0.0.1:1/token'
);

statement error
SELECT * FROM bigquery_query('auth-preflight-project', 'SELECT 1 AS x', use_rest_api=true);
----
<REGEX>:.*BigQuery Authentication Failed.*

statement ok
DROP SECRET bq_auth_preflight_secret;

#################################################################################

statement ok
CREATE SECRET bq_token_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://token-project',
    ACCESS_TOKEN 'ya29.test-access-token-12345'
);

query IIII
SELECT name, type, provider, scope FROM duckdb_secrets() WHERE name = 'bq_token_secret';
----
bq_token_secret	bigquery	config	['bq://token-project']

statement ok
DROP SECRET bq_token_secret;

#################################################################################

statement ok
CREATE SECRET bq_json_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://json-project',
    SERVICE_ACCOUNT_JSON '{"type": "service_account", "project_id": "my-project", "private_key_id": "key123", "private_key": "-----BEGIN PRIVATE KEY-----\nMIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAoIBAQC\n-----END PRIVATE KEY-----\n", "client_email": "test@my-project.iam.gserviceaccount.com", "client_id": "123456789", "auth_uri": "https://accounts.google.com/o/oauth2/auth", "token_uri": "https://oauth2.googleapis.com/token"}'
);

# Verify secret was created
query IIII
SELECT name, type, provider, scope FROM duckdb_secrets() WHERE name = 'bq_json_secret';
----
bq_json_secret	bigquery	config	['bq://json-project']

statement ok
DROP SECRET bq_json_secret;

#################################################################################

statement ok
CREATE SECRET bq_oauth_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-project',
    REFRESH_TOKEN '1//test-refresh-token',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com',
    CLIENT_SECRET 'test-client-secret'
);

query IIII
SELECT name, type, provider, scope FROM duckdb_secrets() WHERE name = 'bq_oauth_secret';
----
bq_oauth_secret	bigquery	config	['bq://oauth-project']

statement ok
DROP SECRET bq_oauth_secret;

#################################################################################

statement ok
CREATE SECRET bq_oauth_token_uri_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-token-uri-project',
    REFRESH_TOKEN '1//test-refresh-token',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com',
    CLIENT_SECRET 'test-client-secret',
    TOKEN_URI 'https://oauth2.googleapis.com/token'
);

query IIII
SELECT name, type, provider, scope FROM duckdb_secrets() WHERE name = 'bq_oauth_token_uri_secret';
----
bq_oauth_token_uri_secret	bigquery	config	['bq://oauth-token-uri-project']

statement ok
DROP SECRET bq_oauth_token_uri_secret;

#################################################################################

# This Key Path doesn't exist
statement error
CREATE SECRET bq_path_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://path-project',
    SERVICE_ACCOUNT_PATH '/this/doesnt/exist.json'
);
----
Invalid Input Error: The 'service_account_path' parameter must be a valid file path (e.g., '/path/to/key.json')

#################################################################################

statement error
CREATE SECRET bq_no_auth_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://no-auth-project'
);
----
Invalid Input Error: BigQuery secret must contain one of: 'access_token', 'service_account_path', 'service_account_json', 'external_account_path', 'external_account_json', or 'refresh_token' + 'client_id' + 'client_secret'

#################################################################################

statement error
CREATE SECRET bq_multiple_auth_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://multi-project',
    ACCESS_TOKEN 'ya29.test-token',
    SERVICE_ACCOUNT_JSON '{"type": "service_account"}'
);
----
Invalid Input Error: BigQuery secret must contain exactly one authentication method

#################################################################################

statement error
CREATE SECRET bq_oauth_missing_refresh_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-error-project',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com',
    CLIENT_SECRET 'test-client-secret'
);
----
Invalid Input Error: BigQuery authorized_user secret is missing required parameter 'refresh_token'

#################################################################################

statement error
CREATE SECRET bq_oauth_missing_client_id_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-error-project',
    REFRESH_TOKEN '1//test-refresh-token',
    CLIENT_SECRET 'test-client-secret'
);
----
Invalid Input Error: BigQuery authorized_user secret is missing required parameter 'client_id'

#################################################################################

statement error
CREATE SECRET bq_oauth_missing_client_secret_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-error-project',
    REFRESH_TOKEN '1//test-refresh-token',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com'
);
----
Invalid Input Error: BigQuery authorized_user secret is missing required parameter 'client_secret'

#################################################################################

statement error
CREATE SECRET bq_oauth_empty_refresh_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-error-project',
    REFRESH_TOKEN '',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com',
    CLIENT_SECRET 'test-client-secret'
);
----
Invalid Input Error: BigQuery authorized_user secret parameter 'refresh_token' must not be empty

#################################################################################

statement error
CREATE SECRET bq_oauth_empty_token_uri_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-error-project',
    REFRESH_TOKEN '1//test-refresh-token',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com',
    CLIENT_SECRET 'test-client-secret',
    TOKEN_URI ''
);
----
Invalid Input Error: BigQuery authorized_user secret parameter 'token_uri' must not be empty

#################################################################################

statement error
CREATE SECRET bq_oauth_multiple_auth_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://oauth-error-project',
    ACCESS_TOKEN 'ya29.test-token',
    REFRESH_TOKEN '1//test-refresh-token',
    CLIENT_ID 'test-client-id.apps.googleusercontent.com',
    CLIENT_SECRET 'test-client-secret'
);
----
Invalid Input Error: BigQuery secret must contain exactly one authentication method

#################################################################################

statement error
CREATE SECRET bq_path_with_json_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://error-project',
    SERVICE_ACCOUNT_PATH '{"type": "service_account", "project_id": "my-project"}'
);
----
Invalid Input Error: The 'service_account_path' parameter must be a valid file path (e.g., '/path/to/key.json')

#################################################################################

statement error
CREATE SECRET bq_json_with_relative_path_secret (
    TYPE bigquery,
    PROVIDER config,
    SCOPE 'bq://error-project',
    SERVICE_ACCOUNT_JSON './keys/service-account.json'
);
----
Invalid Input Error: The 'service_account_json' parameter must be valid JSON content for service account credentials.
